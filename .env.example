# Discord bot token (from Discord Developer Portal → Bot)
BOT_TOKEN=

# Application ID (same page → General Information → Application ID)
CLIENT_ID=

# Optional: register slash commands to this guild only (instant updates while developing).
# Leave empty to register commands globally (can take up to ~1 hour to propagate).
# Same as the server ID
GUILD_ID=
